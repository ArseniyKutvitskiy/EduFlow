services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: eduflow
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports: ["5432:5432"]
    volumes: ["eduflow-data:/var/lib/postgresql/data"]
volumes: { eduflow-data: {} }
